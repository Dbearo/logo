
const Shape = require("./index.js")


describe("Testing test", () => {
    test("demo test", () => {
    Shape.sC = 'blue';
   
      expect(triangle.render()).toEqual(
        'blue'
      );
    });
  });